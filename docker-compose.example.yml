version: "3.8"
services:
    notice_bot:
        image: leontheprofess/aiogram_notice_rabbitmq
        restart: unless-stopped
        env_file: .env
        volumes:
          - logs:/bot/logs
volumes:
  logs: